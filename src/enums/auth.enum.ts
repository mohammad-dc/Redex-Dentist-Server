export enum usersRoles {
  DOCTOR = "doctor",
  PATIENT = "patient",
}

export enum reservationStatus {
  PENDING = "pending",
  APPROVED = "approved",
  DECLINED = "declined",
  CANCELED = "canceled",
}
